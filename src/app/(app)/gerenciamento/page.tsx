import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export default function GerenciamentoPage() {
    return (
        <Card>
            <CardHeader>
                <CardTitle>Gerenciamento de Pendências</CardTitle>
                <CardDescription>
                    Acompanhe e gerencie as pendências das formações.
                </CardDescription>
            </CardHeader>
            <CardContent>
                <p>Página em construção.</p>
            </CardContent>
        </Card>
    );
}
