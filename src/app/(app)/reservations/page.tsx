import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export default function EnsalamentosPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Gerenciamento de Ensalamentos</CardTitle>
        <CardDescription>
          Organize o ensalamento para os eventos das formações.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>Página em construção.</p>
      </CardContent>
    </Card>
  );
}
